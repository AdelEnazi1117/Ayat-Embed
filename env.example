# Quran.Foundation Content API (server-side only)
# - Use "prelive" for development/testing
# - Use "production" for live deployments
QF_ENV=prelive

# OAuth client credentials (keep secret; never expose to the browser)
QF_CLIENT_ID=your_client_id_here
QF_CLIENT_SECRET=your_client_secret_here

# Optional overrides (rarely needed)
# QF_API_BASE_URL=https://apis-prelive.quran.foundation/content/api/v4
# QF_OAUTH_TOKEN_URL=https://prelive-oauth2.quran.foundation/oauth2/token


